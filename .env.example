# Local Development (Docker Compose)
MYSQL_ROOT_PASSWORD=your_password
MYSQL_DATABASE=hms_db
PORT=8080
DATABASE_URL=mysql://root:your_password@mysql:3306/hms_db
JWT_SECRET=your_jwt_secret

# Cloud SQL Connection (for Cloud Run deployment)
# Option 1: Public IP (Working - requires authorized networks in Cloud SQL)
# DATABASE_URL=mysql://USER:PASSWORD@CLOUD_SQL_PUBLIC_IP:3306/hms_db
# Example: DATABASE_URL=mysql://hms_user:password@34.48.21.96:3306/hms_db

# Option 2: Unix Socket (Not working with Prisma MySQL connector)
# DATABASE_URL=mysql://USER:PASSWORD@localhost/hms_db?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME
# Note: Prisma's MySQL connector doesn't properly support Cloud SQL Unix sockets

# Option 3: Cloud SQL Auth Proxy (for local testing with Cloud SQL)
# DATABASE_URL=mysql://USER:PASSWORD@127.0.0.1:3306/hms_db
